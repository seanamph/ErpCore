# 銀行帳戶維護 開發計劃

## 一、功能概述

### 1.1 功能說明
- **功能代碼**: 銀行帳戶維護
- **功能名稱**: 銀行帳戶維護
- **功能描述**: 提供銀行帳戶資料的新增、修改、刪除、查詢功能，包含帳戶編號、銀行代號、帳戶名稱、帳戶號碼、帳戶類型、幣別、狀態等資訊管理
- **參考舊程式**: 
  - `WEB/IMS_CORE/ASP/SYSP000/SYSP271_FI.ASP` (新增)
  - `WEB/IMS_CORE/ASP/SYSP000/SYSP271_FU.ASP` (修改)
  - `WEB/IMS_CORE/ASP/SYSP000/SYSP271_FD.ASP` (刪除)
  - `WEB/IMS_CORE/ASP/SYSP000/SYSP271_FQ.ASP` (查詢)
  - `WEB/IMS_CORE/ASP/SYSP000/SYSP271_FB.ASP` (瀏覽)
  - `WEB/IMS_CORE/ASP/SYSP000/SYSP271_PR.ASP` (報表)

### 1.2 業務需求
- 管理系統銀行帳戶資訊
- 支援銀行帳戶的新增、修改、刪除、查詢
- 記錄銀行帳戶的建立與變更資訊
- 支援帳戶類型分類（活期、定期、外幣等）
- 支援幣別設定
- 支援帳戶狀態管理（啟用/停用）
- 支援帳戶餘額查詢
- 支援多銀行帳戶管理
- 與供應商、付款單等模組整合

---

## 二、資料庫設計 (Schema)

### 2.1 主要資料表: `BankAccounts` (對應舊系統 `BANK_ACCT`)

```sql
CREATE TABLE [dbo].[BankAccounts] (
    [TKey] BIGINT NOT NULL PRIMARY KEY IDENTITY(1,1),
    [AcctId] NVARCHAR(50) NOT NULL, -- 帳戶編號 (ACCT_ID)
    [BankId] NVARCHAR(50) NOT NULL, -- 銀行代號 (BANK_ID)
    [AcctName] NVARCHAR(100) NOT NULL, -- 帳戶名稱 (ACCT_NAME)
    [AcctNo] NVARCHAR(50) NOT NULL, -- 帳戶號碼 (ACCT_NO)
    [AcctType] NVARCHAR(20) NULL, -- 帳戶類型 (ACCT_TYPE) 1:活期, 2:定期, 3:外幣
    [Currency] NVARCHAR(10) NULL DEFAULT 'TWD', -- 幣別 (CURRENCY)
    [Status] NVARCHAR(10) NOT NULL DEFAULT '1', -- 狀態 (STATUS) 1:啟用, 0:停用
    [Balance] DECIMAL(18, 2) NULL DEFAULT 0, -- 帳戶餘額 (BALANCE)
    [OpeningDate] DATETIME2 NULL, -- 開戶日期 (OPENING_DATE)
    [ClosingDate] DATETIME2 NULL, -- 結清日期 (CLOSING_DATE)
    [ContactPerson] NVARCHAR(50) NULL, -- 聯絡人 (CONTACT_PERSON)
    [ContactPhone] NVARCHAR(50) NULL, -- 聯絡電話 (CONTACT_PHONE)
    [ContactEmail] NVARCHAR(100) NULL, -- 聯絡信箱 (CONTACT_EMAIL)
    [BranchName] NVARCHAR(100) NULL, -- 分行名稱 (BRANCH_NAME)
    [BranchCode] NVARCHAR(50) NULL, -- 分行代號 (BRANCH_CODE)
    [SwiftCode] NVARCHAR(50) NULL, -- SWIFT代號 (SWIFT_CODE)
    [Notes] NVARCHAR(500) NULL, -- 備註 (NOTES)
    [CreatedBy] NVARCHAR(50) NULL, -- 建立者 (BUSER)
    [CreatedAt] DATETIME2 NOT NULL DEFAULT GETDATE(), -- 建立時間 (BTIME)
    [UpdatedBy] NVARCHAR(50) NULL, -- 更新者 (CUSER)
    [UpdatedAt] DATETIME2 NOT NULL DEFAULT GETDATE(), -- 更新時間 (CTIME)
    [CreatedPriority] INT NULL, -- 建立者等級 (CPRIORITY)
    [CreatedGroup] NVARCHAR(50) NULL, -- 建立者群組 (CGROUP)
    CONSTRAINT [PK_BankAccounts] PRIMARY KEY CLUSTERED ([TKey] ASC),
    CONSTRAINT [UQ_BankAccounts_AcctId] UNIQUE ([AcctId]),
    CONSTRAINT [FK_BankAccounts_Banks] FOREIGN KEY ([BankId]) REFERENCES [dbo].[Banks] ([BankId])
);

-- 索引
CREATE NONCLUSTERED INDEX [IX_BankAccounts_AcctId] ON [dbo].[BankAccounts] ([AcctId]);
CREATE NONCLUSTERED INDEX [IX_BankAccounts_BankId] ON [dbo].[BankAccounts] ([BankId]);
CREATE NONCLUSTERED INDEX [IX_BankAccounts_AcctNo] ON [dbo].[BankAccounts] ([AcctNo]);
CREATE NONCLUSTERED INDEX [IX_BankAccounts_Status] ON [dbo].[BankAccounts] ([Status]);
CREATE NONCLUSTERED INDEX [IX_BankAccounts_AcctType] ON [dbo].[BankAccounts] ([AcctType]);
CREATE NONCLUSTERED INDEX [IX_BankAccounts_Currency] ON [dbo].[BankAccounts] ([Currency]);
```

### 2.2 相關資料表

#### 2.2.1 `Banks` - 銀行主檔
```sql
-- 用於查詢銀行列表
-- 參考: `開發計劃/02-基本資料管理/03-金融機構/SYSBC20-銀行基本資料維護作業.md`
```

#### 2.2.2 `PaymentOrders` - 付款單主檔
```sql
-- 用於查詢付款單與銀行帳戶的關聯
-- 參考付款單設計
```

### 2.3 資料字典

| 欄位名稱 | 資料類型 | 長度 | 允許NULL | 預設值 | 說明 | 備註 |
|---------|---------|------|---------|--------|------|------|
| TKey | BIGINT | - | NO | IDENTITY(1,1) | 主鍵 | 自動遞增 |
| AcctId | NVARCHAR | 50 | NO | - | 帳戶編號 | 唯一，主鍵候選 |
| BankId | NVARCHAR | 50 | NO | - | 銀行代號 | 外鍵至Banks表 |
| AcctName | NVARCHAR | 100 | NO | - | 帳戶名稱 | - |
| AcctNo | NVARCHAR | 50 | NO | - | 帳戶號碼 | - |
| AcctType | NVARCHAR | 20 | YES | - | 帳戶類型 | 1:活期, 2:定期, 3:外幣 |
| Currency | NVARCHAR | 10 | YES | 'TWD' | 幣別 | - |
| Status | NVARCHAR | 10 | NO | '1' | 狀態 | 1:啟用, 0:停用 |
| Balance | DECIMAL | 18,2 | YES | 0 | 帳戶餘額 | - |
| OpeningDate | DATETIME2 | - | YES | - | 開戶日期 | - |
| ClosingDate | DATETIME2 | - | YES | - | 結清日期 | - |
| ContactPerson | NVARCHAR | 50 | YES | - | 聯絡人 | - |
| ContactPhone | NVARCHAR | 50 | YES | - | 聯絡電話 | - |
| ContactEmail | NVARCHAR | 100 | YES | - | 聯絡信箱 | - |
| BranchName | NVARCHAR | 100 | YES | - | 分行名稱 | - |
| BranchCode | NVARCHAR | 50 | YES | - | 分行代號 | - |
| SwiftCode | NVARCHAR | 50 | YES | - | SWIFT代號 | - |
| Notes | NVARCHAR | 500 | YES | - | 備註 | - |
| CreatedBy | NVARCHAR | 50 | YES | - | 建立者 | - |
| CreatedAt | DATETIME2 | - | NO | GETDATE() | 建立時間 | - |
| UpdatedBy | NVARCHAR | 50 | YES | - | 更新者 | - |
| UpdatedAt | DATETIME2 | - | NO | GETDATE() | 更新時間 | - |
| CreatedPriority | INT | - | YES | - | 建立者等級 | - |
| CreatedGroup | NVARCHAR | 50 | YES | - | 建立者群組 | - |

---

## 三、後端 API 設計

### 3.1 API 端點列表

#### 3.1.1 查詢銀行帳戶列表
- **HTTP 方法**: `GET`
- **路徑**: `/api/v1/bank-accounts`
- **說明**: 查詢銀行帳戶列表，支援分頁、排序、篩選
- **請求參數**:
  ```json
  {
    "pageIndex": 1,
    "pageSize": 20,
    "sortField": "AcctId",
    "sortOrder": "ASC",
    "filters": {
      "acctId": "",
      "bankId": "",
      "acctName": "",
      "acctNo": "",
      "acctType": "",
      "currency": "",
      "status": ""
    }
  }
  ```
- **回應格式**:
  ```json
  {
    "success": true,
    "code": 200,
    "message": "查詢成功",
    "data": {
      "items": [
        {
          "tKey": 1,
          "acctId": "ACCT001",
          "bankId": "B001",
          "bankName": "台灣銀行",
          "acctName": "主要營運帳戶",
          "acctNo": "1234567890123",
          "acctType": "1",
          "acctTypeName": "活期",
          "currency": "TWD",
          "status": "1",
          "statusName": "啟用",
          "balance": 1000000.00,
          "openingDate": "2020-01-01",
          "contactPerson": "張三",
          "contactPhone": "02-12345678",
          "contactEmail": "contact@example.com",
          "branchName": "台北分行",
          "branchCode": "001",
          "swiftCode": "BKTWTWTP"
        }
      ],
      "totalCount": 100,
      "pageIndex": 1,
      "pageSize": 20,
      "totalPages": 5
    },
    "timestamp": "2024-01-01T00:00:00Z"
  }
  ```

#### 3.1.2 查詢單筆銀行帳戶
- **HTTP 方法**: `GET`
- **路徑**: `/api/v1/bank-accounts/{acctId}`
- **說明**: 根據帳戶編號查詢單筆銀行帳戶資料
- **路徑參數**:
  - `acctId`: 帳戶編號
- **回應格式**:
  ```json
  {
    "success": true,
    "code": 200,
    "message": "查詢成功",
    "data": {
      "tKey": 1,
      "acctId": "ACCT001",
      "bankId": "B001",
      "bankName": "台灣銀行",
      "acctName": "主要營運帳戶",
      "acctNo": "1234567890123",
      "acctType": "1",
      "currency": "TWD",
      "status": "1",
      "balance": 1000000.00,
      "openingDate": "2020-01-01",
      "closingDate": null,
      "contactPerson": "張三",
      "contactPhone": "02-12345678",
      "contactEmail": "contact@example.com",
      "branchName": "台北分行",
      "branchCode": "001",
      "swiftCode": "BKTWTWTP",
      "notes": "備註"
    },
    "timestamp": "2024-01-01T00:00:00Z"
  }
  ```

#### 3.1.3 新增銀行帳戶
- **HTTP 方法**: `POST`
- **路徑**: `/api/v1/bank-accounts`
- **說明**: 新增銀行帳戶資料
- **請求格式**:
  ```json
  {
    "acctId": "ACCT001",
    "bankId": "B001",
    "acctName": "主要營運帳戶",
    "acctNo": "1234567890123",
    "acctType": "1",
    "currency": "TWD",
    "status": "1",
    "balance": 0,
    "openingDate": "2024-01-01",
    "closingDate": null,
    "contactPerson": "張三",
    "contactPhone": "02-12345678",
    "contactEmail": "contact@example.com",
    "branchName": "台北分行",
    "branchCode": "001",
    "swiftCode": "BKTWTWTP",
    "notes": "備註"
  }
  ```
- **回應格式**:
  ```json
  {
    "success": true,
    "code": 200,
    "message": "新增成功",
    "data": {
      "acctId": "ACCT001"
    },
    "timestamp": "2024-01-01T00:00:00Z"
  }
  ```

#### 3.1.4 修改銀行帳戶
- **HTTP 方法**: `PUT`
- **路徑**: `/api/v1/bank-accounts/{acctId}`
- **說明**: 修改銀行帳戶資料
- **路徑參數**:
  - `acctId`: 帳戶編號
- **請求格式**: 同新增，但 `acctId` 不可修改
- **回應格式**: 同新增

#### 3.1.5 刪除銀行帳戶
- **HTTP 方法**: `DELETE`
- **路徑**: `/api/v1/bank-accounts/{acctId}`
- **說明**: 刪除銀行帳戶資料（軟刪除或硬刪除）
- **路徑參數**:
  - `acctId`: 帳戶編號
- **回應格式**:
  ```json
  {
    "success": true,
    "code": 200,
    "message": "刪除成功",
    "data": null,
    "timestamp": "2024-01-01T00:00:00Z"
  }
  ```

#### 3.1.6 批次刪除銀行帳戶
- **HTTP 方法**: `DELETE`
- **路徑**: `/api/v1/bank-accounts/batch`
- **說明**: 批次刪除多筆銀行帳戶
- **請求格式**:
  ```json
  {
    "acctIds": ["ACCT001", "ACCT002", "ACCT003"]
  }
  ```

#### 3.1.7 啟用/停用銀行帳戶
- **HTTP 方法**: `PUT`
- **路徑**: `/api/v1/bank-accounts/{acctId}/status`
- **說明**: 啟用或停用銀行帳戶
- **請求格式**:
  ```json
  {
    "status": "1" // 1:啟用, 0:停用
  }
  ```

#### 3.1.8 查詢帳戶餘額
- **HTTP 方法**: `GET`
- **路徑**: `/api/v1/bank-accounts/{acctId}/balance`
- **說明**: 查詢銀行帳戶餘額
- **回應格式**:
  ```json
  {
    "success": true,
    "code": 200,
    "message": "查詢成功",
    "data": {
      "acctId": "ACCT001",
      "acctName": "主要營運帳戶",
      "balance": 1000000.00,
      "currency": "TWD",
      "lastUpdateDate": "2024-01-01T10:00:00"
    },
    "timestamp": "2024-01-01T00:00:00Z"
  }
  ```

### 3.2 後端實作類別

#### 3.2.1 Controller: `BankAccountsController.cs`
```csharp
namespace RSL.IMS3.Api.Controllers
{
    [ApiController]
    [Route("api/v1/bank-accounts")]
    [Authorize]
    public class BankAccountsController : ControllerBase
    {
        private readonly IBankAccountService _bankAccountService;
        
        public BankAccountsController(IBankAccountService bankAccountService)
        {
            _bankAccountService = bankAccountService;
        }
        
        [HttpGet]
        public async Task<ActionResult<ApiResponse<PagedResult<BankAccountDto>>>> GetBankAccounts([FromQuery] BankAccountQueryDto query)
        {
            // 實作查詢邏輯
        }
        
        [HttpGet("{acctId}")]
        public async Task<ActionResult<ApiResponse<BankAccountDto>>> GetBankAccount(string acctId)
        {
            // 實作查詢單筆邏輯
        }
        
        [HttpPost]
        public async Task<ActionResult<ApiResponse<string>>> CreateBankAccount([FromBody] CreateBankAccountDto dto)
        {
            // 實作新增邏輯
        }
        
        [HttpPut("{acctId}")]
        public async Task<ActionResult<ApiResponse>> UpdateBankAccount(string acctId, [FromBody] UpdateBankAccountDto dto)
        {
            // 實作修改邏輯
        }
        
        [HttpDelete("{acctId}")]
        public async Task<ActionResult<ApiResponse>> DeleteBankAccount(string acctId)
        {
            // 實作刪除邏輯
        }
        
        [HttpPut("{acctId}/status")]
        public async Task<ActionResult<ApiResponse>> UpdateStatus(string acctId, [FromBody] UpdateStatusDto dto)
        {
            // 實作啟用/停用邏輯
        }
        
        [HttpGet("{acctId}/balance")]
        public async Task<ActionResult<ApiResponse<BankAccountBalanceDto>>> GetBalance(string acctId)
        {
            // 實作查詢餘額邏輯
        }
    }
}
```

#### 3.2.2 Service: `BankAccountService.cs`
```csharp
namespace RSL.IMS3.Application.Services
{
    public interface IBankAccountService
    {
        Task<PagedResult<BankAccountDto>> GetBankAccountsAsync(BankAccountQueryDto query);
        Task<BankAccountDto> GetBankAccountByIdAsync(string acctId);
        Task<string> CreateBankAccountAsync(CreateBankAccountDto dto);
        Task UpdateBankAccountAsync(string acctId, UpdateBankAccountDto dto);
        Task DeleteBankAccountAsync(string acctId);
        Task UpdateStatusAsync(string acctId, string status);
        Task<BankAccountBalanceDto> GetBalanceAsync(string acctId);
    }
}
```

#### 3.2.3 Repository: `BankAccountRepository.cs`
```csharp
namespace RSL.IMS3.Infrastructure.Data.Repositories
{
    public interface IBankAccountRepository
    {
        Task<BankAccount> GetByIdAsync(string acctId);
        Task<PagedResult<BankAccount>> GetPagedAsync(BankAccountQuery query);
        Task<BankAccount> CreateAsync(BankAccount bankAccount);
        Task<BankAccount> UpdateAsync(BankAccount bankAccount);
        Task DeleteAsync(string acctId);
        Task<bool> ExistsAsync(string acctId);
        Task<bool> ExistsByAcctNoAsync(string acctNo);
    }
}
```

---

## 四、前端 UI 設計

### 4.1 頁面結構

#### 4.1.1 銀行帳戶列表頁面 (`BankAccountList.vue`)
- **路徑**: `/procurement/bank-accounts`
- **功能**: 顯示銀行帳戶列表，支援查詢、新增、修改、刪除
- **主要元件**:
  - 查詢表單 (BankAccountSearchForm)
  - 資料表格 (BankAccountDataTable)
  - 新增/修改對話框 (BankAccountDialog)
  - 刪除確認對話框

#### 4.1.2 銀行帳戶詳細頁面 (`BankAccountDetail.vue`)
- **路徑**: `/procurement/bank-accounts/:acctId`
- **功能**: 顯示銀行帳戶詳細資料，支援修改

### 4.2 UI 元件設計

#### 4.2.1 查詢表單元件 (`BankAccountSearchForm.vue`)
```vue
<template>
  <el-form :model="searchForm" inline>
    <el-form-item label="帳戶編號">
      <el-input v-model="searchForm.acctId" placeholder="請輸入帳戶編號" />
    </el-form-item>
    <el-form-item label="銀行代號">
      <el-select v-model="searchForm.bankId" placeholder="請選擇銀行" filterable>
        <el-option v-for="bank in bankList" :key="bank.bankId" :label="bank.bankName" :value="bank.bankId" />
      </el-select>
    </el-form-item>
    <el-form-item label="帳戶名稱">
      <el-input v-model="searchForm.acctName" placeholder="請輸入帳戶名稱" />
    </el-form-item>
    <el-form-item label="帳戶號碼">
      <el-input v-model="searchForm.acctNo" placeholder="請輸入帳戶號碼" />
    </el-form-item>
    <el-form-item label="帳戶類型">
      <el-select v-model="searchForm.acctType" placeholder="請選擇帳戶類型">
        <el-option label="活期" value="1" />
        <el-option label="定期" value="2" />
        <el-option label="外幣" value="3" />
      </el-select>
    </el-form-item>
    <el-form-item label="幣別">
      <el-select v-model="searchForm.currency" placeholder="請選擇幣別">
        <el-option label="新台幣" value="TWD" />
        <el-option label="美元" value="USD" />
        <el-option label="人民幣" value="CNY" />
      </el-select>
    </el-form-item>
    <el-form-item label="狀態">
      <el-select v-model="searchForm.status" placeholder="請選擇狀態">
        <el-option label="啟用" value="1" />
        <el-option label="停用" value="0" />
      </el-select>
    </el-form-item>
    <el-form-item>
      <el-button type="primary" @click="handleSearch">查詢</el-button>
      <el-button @click="handleReset">重置</el-button>
    </el-form-item>
  </el-form>
</template>
```

#### 4.2.2 資料表格元件 (`BankAccountDataTable.vue`)
```vue
<template>
  <div>
    <el-table :data="bankAccountList" v-loading="loading">
      <el-table-column prop="acctId" label="帳戶編號" width="120" />
      <el-table-column prop="bankName" label="銀行名稱" width="150" />
      <el-table-column prop="acctName" label="帳戶名稱" width="150" />
      <el-table-column prop="acctNo" label="帳戶號碼" width="150" />
      <el-table-column prop="acctTypeName" label="帳戶類型" width="100" />
      <el-table-column prop="currency" label="幣別" width="80" />
      <el-table-column prop="balance" label="餘額" width="120" align="right">
        <template #default="{ row }">
          {{ formatCurrency(row.balance, row.currency) }}
        </template>
      </el-table-column>
      <el-table-column prop="status" label="狀態" width="80">
        <template #default="{ row }">
          <el-tag :type="getStatusType(row.status)">
            {{ getStatusText(row.status) }}
          </el-tag>
        </template>
      </el-table-column>
      <el-table-column prop="contactPerson" label="聯絡人" width="100" />
      <el-table-column prop="contactPhone" label="聯絡電話" width="120" />
      <el-table-column label="操作" width="200" fixed="right">
        <template #default="{ row }">
          <el-button type="primary" size="small" @click="handleEdit(row)">修改</el-button>
          <el-button type="danger" size="small" @click="handleDelete(row)">刪除</el-button>
          <el-button type="info" size="small" @click="handleViewBalance(row)">查詢餘額</el-button>
        </template>
      </el-table-column>
    </el-table>
    <el-pagination
      v-model:current-page="pagination.pageIndex"
      v-model:page-size="pagination.pageSize"
      :total="pagination.totalCount"
      :page-sizes="[10, 20, 50, 100]"
      layout="total, sizes, prev, pager, next, jumper"
      @size-change="handleSizeChange"
      @current-change="handlePageChange"
    />
  </div>
</template>
```

#### 4.2.3 新增/修改對話框 (`BankAccountDialog.vue`)
```vue
<template>
  <el-dialog
    :title="dialogTitle"
    v-model="visible"
    width="900px"
    @close="handleClose"
  >
    <el-form :model="form" :rules="rules" ref="formRef" label-width="120px">
      <el-row :gutter="20">
        <el-col :span="12">
          <el-form-item label="帳戶編號" prop="acctId">
            <el-input v-model="form.acctId" :disabled="isEdit" placeholder="請輸入帳戶編號" />
          </el-form-item>
        </el-col>
        <el-col :span="12">
          <el-form-item label="銀行代號" prop="bankId">
            <el-select v-model="form.bankId" placeholder="請選擇銀行" filterable>
              <el-option v-for="bank in bankList" :key="bank.bankId" :label="bank.bankName" :value="bank.bankId" />
            </el-select>
          </el-form-item>
        </el-col>
      </el-row>
      <el-row :gutter="20">
        <el-col :span="12">
          <el-form-item label="帳戶名稱" prop="acctName">
            <el-input v-model="form.acctName" placeholder="請輸入帳戶名稱" />
          </el-form-item>
        </el-col>
        <el-col :span="12">
          <el-form-item label="帳戶號碼" prop="acctNo">
            <el-input v-model="form.acctNo" placeholder="請輸入帳戶號碼" />
          </el-form-item>
        </el-col>
      </el-row>
      <el-row :gutter="20">
        <el-col :span="12">
          <el-form-item label="帳戶類型" prop="acctType">
            <el-select v-model="form.acctType" placeholder="請選擇帳戶類型">
              <el-option label="活期" value="1" />
              <el-option label="定期" value="2" />
              <el-option label="外幣" value="3" />
            </el-select>
          </el-form-item>
        </el-col>
        <el-col :span="12">
          <el-form-item label="幣別" prop="currency">
            <el-select v-model="form.currency" placeholder="請選擇幣別">
              <el-option label="新台幣" value="TWD" />
              <el-option label="美元" value="USD" />
              <el-option label="人民幣" value="CNY" />
            </el-select>
          </el-form-item>
        </el-col>
      </el-row>
      <el-row :gutter="20">
        <el-col :span="12">
          <el-form-item label="開戶日期" prop="openingDate">
            <el-date-picker v-model="form.openingDate" type="date" placeholder="請選擇日期" />
          </el-form-item>
        </el-col>
        <el-col :span="12">
          <el-form-item label="結清日期" prop="closingDate">
            <el-date-picker v-model="form.closingDate" type="date" placeholder="請選擇日期" />
          </el-form-item>
        </el-col>
      </el-row>
      <el-row :gutter="20">
        <el-col :span="12">
          <el-form-item label="分行名稱" prop="branchName">
            <el-input v-model="form.branchName" placeholder="請輸入分行名稱" />
          </el-form-item>
        </el-col>
        <el-col :span="12">
          <el-form-item label="分行代號" prop="branchCode">
            <el-input v-model="form.branchCode" placeholder="請輸入分行代號" />
          </el-form-item>
        </el-col>
      </el-row>
      <el-row :gutter="20">
        <el-col :span="12">
          <el-form-item label="SWIFT代號" prop="swiftCode">
            <el-input v-model="form.swiftCode" placeholder="請輸入SWIFT代號" />
          </el-form-item>
        </el-col>
        <el-col :span="12">
          <el-form-item label="狀態" prop="status">
            <el-select v-model="form.status" placeholder="請選擇狀態">
              <el-option label="啟用" value="1" />
              <el-option label="停用" value="0" />
            </el-select>
          </el-form-item>
        </el-col>
      </el-row>
      <el-row :gutter="20">
        <el-col :span="12">
          <el-form-item label="聯絡人" prop="contactPerson">
            <el-input v-model="form.contactPerson" placeholder="請輸入聯絡人" />
          </el-form-item>
        </el-col>
        <el-col :span="12">
          <el-form-item label="聯絡電話" prop="contactPhone">
            <el-input v-model="form.contactPhone" placeholder="請輸入聯絡電話" />
          </el-form-item>
        </el-col>
      </el-row>
      <el-form-item label="聯絡信箱" prop="contactEmail">
        <el-input v-model="form.contactEmail" placeholder="請輸入聯絡信箱" />
      </el-form-item>
      <el-form-item label="備註" prop="notes">
        <el-input v-model="form.notes" type="textarea" :rows="3" placeholder="請輸入備註" />
      </el-form-item>
    </el-form>
    <template #footer>
      <el-button @click="handleClose">取消</el-button>
      <el-button type="primary" @click="handleSubmit">確定</el-button>
    </template>
  </el-dialog>
</template>
```

### 4.3 API 呼叫 (`bankAccount.api.ts`)
```typescript
import request from '@/utils/request';

export interface BankAccountDto {
  tKey: number;
  acctId: string;
  bankId: string;
  bankName?: string;
  acctName: string;
  acctNo: string;
  acctType?: string;
  acctTypeName?: string;
  currency?: string;
  status: string;
  statusName?: string;
  balance?: number;
  openingDate?: string;
  closingDate?: string;
  contactPerson?: string;
  contactPhone?: string;
  contactEmail?: string;
  branchName?: string;
  branchCode?: string;
  swiftCode?: string;
  notes?: string;
}

export interface BankAccountQueryDto {
  pageIndex: number;
  pageSize: number;
  sortField?: string;
  sortOrder?: 'ASC' | 'DESC';
  filters?: {
    acctId?: string;
    bankId?: string;
    acctName?: string;
    acctNo?: string;
    acctType?: string;
    currency?: string;
    status?: string;
  };
}

export interface CreateBankAccountDto {
  acctId: string;
  bankId: string;
  acctName: string;
  acctNo: string;
  acctType?: string;
  currency?: string;
  status: string;
  balance?: number;
  openingDate?: string;
  closingDate?: string;
  contactPerson?: string;
  contactPhone?: string;
  contactEmail?: string;
  branchName?: string;
  branchCode?: string;
  swiftCode?: string;
  notes?: string;
}

export interface UpdateBankAccountDto extends Omit<CreateBankAccountDto, 'acctId'> {}

export interface BankAccountBalanceDto {
  acctId: string;
  acctName: string;
  balance: number;
  currency: string;
  lastUpdateDate: string;
}

// API 函數
export const getBankAccountList = (query: BankAccountQueryDto) => {
  return request.get<ApiResponse<PagedResult<BankAccountDto>>>('/api/v1/bank-accounts', { params: query });
};

export const getBankAccountById = (acctId: string) => {
  return request.get<ApiResponse<BankAccountDto>>(`/api/v1/bank-accounts/${acctId}`);
};

export const createBankAccount = (data: CreateBankAccountDto) => {
  return request.post<ApiResponse<string>>('/api/v1/bank-accounts', data);
};

export const updateBankAccount = (acctId: string, data: UpdateBankAccountDto) => {
  return request.put<ApiResponse>(`/api/v1/bank-accounts/${acctId}`, data);
};

export const deleteBankAccount = (acctId: string) => {
  return request.delete<ApiResponse>(`/api/v1/bank-accounts/${acctId}`);
};

export const updateStatus = (acctId: string, status: string) => {
  return request.put<ApiResponse>(`/api/v1/bank-accounts/${acctId}/status`, { status });
};

export const getBalance = (acctId: string) => {
  return request.get<ApiResponse<BankAccountBalanceDto>>(`/api/v1/bank-accounts/${acctId}/balance`);
};
```

---

## 五、開發時程

### 5.1 階段一: 資料庫設計 (1天)
- [ ] 建立資料表結構
- [ ] 建立索引
- [ ] 建立外鍵約束
- [ ] 建立預設值
- [ ] 資料庫遷移腳本

### 5.2 階段二: 後端開發 (3天)
- [ ] Entity 類別建立
- [ ] Repository 實作
- [ ] Service 實作
- [ ] Controller 實作
- [ ] DTO 類別建立
- [ ] 驗證邏輯實作
- [ ] 單元測試

### 5.3 階段三: 前端開發 (3天)
- [ ] API 呼叫函數
- [ ] 列表頁面開發
- [ ] 新增/修改對話框開發
- [ ] 查詢表單開發
- [ ] 資料表格開發
- [ ] 表單驗證
- [ ] 元件測試

### 5.4 階段四: 整合測試 (1天)
- [ ] API 整合測試
- [ ] 端對端測試
- [ ] 效能測試
- [ ] 安全性測試

### 5.5 階段五: 文件與部署 (1天)
- [ ] API 文件更新
- [ ] 使用者手冊
- [ ] 部署文件

**總計**: 9天

---

## 六、注意事項

### 6.1 安全性
- 帳戶號碼等敏感資料必須加密儲存
- 必須實作權限檢查
- 必須記錄操作日誌

### 6.2 效能
- 大量資料查詢必須使用分頁
- 必須建立適當的索引
- 必須使用快取機制

### 6.3 資料驗證
- 帳戶編號必須唯一
- 帳戶號碼必須唯一
- 必填欄位必須驗證
- 日期範圍必須驗證
- 狀態值必須在允許範圍內
- 銀行代號必須存在

### 6.4 業務邏輯
- 刪除銀行帳戶前必須檢查是否有相關付款單
- 停用銀行帳戶時必須檢查是否有進行中的付款
- 帳戶餘額必須即時更新
- 開戶日期不能晚於結清日期

---

## 七、測試案例

### 7.1 單元測試
- [ ] 新增銀行帳戶成功
- [ ] 新增銀行帳戶失敗 (重複編號)
- [ ] 新增銀行帳戶失敗 (重複帳戶號碼)
- [ ] 修改銀行帳戶成功
- [ ] 修改銀行帳戶失敗 (不存在)
- [ ] 刪除銀行帳戶成功
- [ ] 刪除銀行帳戶失敗 (有相關付款單)
- [ ] 查詢銀行帳戶列表成功
- [ ] 查詢單筆銀行帳戶成功
- [ ] 啟用/停用銀行帳戶成功
- [ ] 查詢帳戶餘額成功

### 7.2 整合測試
- [ ] 完整 CRUD 流程測試
- [ ] 權限檢查測試
- [ ] 資料驗證測試
- [ ] 錯誤處理測試
- [ ] 與付款單模組整合測試

### 7.3 效能測試
- [ ] 大量資料查詢測試
- [ ] 並發操作測試

---

## 八、參考資料

### 8.1 舊程式碼
- `WEB/IMS_CORE/ASP/SYSP000/SYSP271_FI.ASP` - 新增頁面
- `WEB/IMS_CORE/ASP/SYSP000/SYSP271_FU.ASP` - 修改頁面
- `WEB/IMS_CORE/ASP/SYSP000/SYSP271_FD.ASP` - 刪除頁面
- `WEB/IMS_CORE/ASP/SYSP000/SYSP271_FQ.ASP` - 查詢頁面
- `WEB/IMS_CORE/ASP/SYSP000/SYSP271_FB.ASP` - 瀏覽頁面
- `WEB/IMS_CORE/ASP/SYSP000/SYSP271_PR.ASP` - 報表頁面

### 8.2 資料庫 Schema
- 舊系統: `BANK_ACCT` 表
- 相關表: `BANK` (銀行主檔)

---

**文件版本**: v1.0  
**建立日期**: 2024-01-01  
**最後更新**: 2024-01-01

