# ErpCore Web Dockerfile
# 用途: 建置 ErpCore Vue 前端的 Docker 映像

# 建置階段
FROM node:18-alpine AS build

WORKDIR /app

# 複製 package 檔案
COPY src/ErpCore.Web/package*.json ./

# 安裝依賴
RUN npm ci

# 複製原始碼
COPY src/ErpCore.Web/ .

# 建置應用程式
RUN npm run build

# 生產階段
FROM nginx:alpine

# 複製建置結果到 nginx
COPY --from=build /app/dist /usr/share/nginx/html

# 複製 nginx 設定檔 (如果有的話)
# COPY src/ErpCore.Web/nginx.conf /etc/nginx/conf.d/default.conf

# 暴露端口
EXPOSE 80

# 啟動 nginx
CMD ["nginx", "-g", "daemon off;"]

